---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: 'e7546754-5878-48ff-9873-084b2c3ca6b9'
  PropagateID: 'e7546754-5878-48ff-9873-084b2c3ca6b9'
  ReservedCode1: '495a397b-3c6c-4cc5-aebe-1654c4ad608d'
  ReservedCode2: '495a397b-3c6c-4cc5-aebe-1654c4ad608d'
---

# 鼠智赛事通 · 第二阶段开发报告

**报告日期：2026-09-22**

**项目定位：** 上海大学游泳队成绩管理系统（GitHub Pages 部署）

**本阶段范围：** ①运动员技术等级查询 ②数据删除功能（运动员/比赛/成绩）③跨设备数据同步专项技术研究（暂停实现，仅输出研究报告）

**交付产物：** 功能代码（已提交推送，commit `88c5b31`）、本报告（含功能完成情况、localStorage 架构说明、四类同步方案可行性分析与六方案对比表、下一步实现路线）

**系统线上地址：** https://wendy42-1.github.io/SHU-Swimming-Club/

**仓库：** wendy42-1/SHU-Swimming-Club（main 分支，公共仓库）

---

## 一、项目概览

### 1.1 技术栈

| 项目 | 说明 |
|------|------|
| 前端 | 原生 HTML5 + CSS3 + JavaScript（ES Modules），无第三方框架 |
| 数据 | JSON 静态文件（`data/*.json`）+ 浏览器 localStorage 覆盖层 |
| 部署 | GitHub Pages（静态托管） |
| 数据校验 | GitHub Actions `validate-data.yml`（JSON 语法 / ID 唯一 / 外键引用 / timeMs 非负） |
| 自动部署 | GitHub Actions `deploy.yml` |

### 1.2 角色与权限（演示级）

| 角色 | 权限要点 |
|------|----------|
| SUPER_ADMIN（超级管理员） | 全部权限，含运动员/比赛的删除 |
| SCORE_ADMIN（成绩管理员） | 新增/编辑成绩，无删除权限 |
| VIEWER（普通用户） | 仅查看 |

> 说明：当前角色控制为前端演示级权限（sessionStorage 切换），仅控制 UI 显示/隐藏，不构成生产级安全认证，详见第七节。

### 1.3 本阶段版本基线

- 提交：`88c5b31`（feat: 运动员技术等级查询+删除功能）
- 变更规模：9 个文件，+1273 行

---

## 二、功能完成情况（PASS 清单）

以下功能均已完成开发并通过浏览器实测（本地 http.server + 线上 GitHub Pages 双环境验证）。

### 2.1 运动员管理

| 功能 | 状态 | 说明 |
|------|------|------|
| 新增运动员 | PASS | SUPER_ADMIN 可新增，自动分配 ID，写入 localStorage |
| 编辑运动员 | PASS | 修改姓名/性别/分组 |
| 停用运动员（软删除） | PASS | 有历史成绩的运动员仅可停用（status=inactive），历史成绩保留 |
| 运动员物理删除 | 禁用 | 有成绩记录时禁止物理删除（防止外键悬空），仅停用 |

### 2.2 比赛管理

| 功能 | 状态 | 说明 |
|------|------|------|
| 新增比赛 | PASS | 名称/日期必填，自动分配 ID |
| 编辑比赛 | PASS | 名称/日期/地点编辑 |
| 停用比赛（软删除） | PASS | 有成绩的比赛仅可停用（status=inactive） |
| 物理删除比赛 | PASS | 仅限**无任何成绩**的比赛，删除前二次确认 |

### 2.3 成绩管理

| 功能 | 状态 | 说明 |
|------|------|------|
| 新增成绩 | PASS | 支持 `58.32` 与 `1:02.35` 两种输入格式，统一存毫秒整数 |
| 编辑成绩 | PASS | 修改成绩/状态，记录 previousValues 与 updateReason |
| 标记 DQ | PASS | 成绩标为 DQ（保留记录） |
| 软删除成绩 | PASS | 标记 status=deleted，保留完整历史，不物理删除 |
| 成绩筛选 | PASS | 支持按状态（含“已删除”）筛选显示 |

### 2.4 运动员技术等级查询（本阶段新增）

| 功能 | 状态 | 说明 |
|------|------|------|
| 等级标准数据读取 | PASS | 18 个小项 × 男/女 × 50米池/25米池，共 72 组标准 |
| 等级查询 | PASS | 输入性别/项目/池型/成绩 → 返回达标等级及各等级对照 |
| 达标等级展示 | PASS | 高亮最高等级，显示与各等级标准的时间差（快/差） |
| 无标准项处理 | PASS | 男子 100 米混合泳 50 米池无国际级标准时显示“—” |
| 项目下拉 | PASS | 18 个可查询小项自动加载 |
| 错误输入处理 | PASS | 非法成绩格式提示错误，不崩溃 |

### 2.5 导航与页面

| 功能 | 状态 | 说明 |
|------|------|------|
| 等级查询页面 | PASS | 独立页面 `pages/athlete-grade.html` |
| 导航入口 | PASS | 顶部导航新增“等级查询”，所有角色可见 |

---

## 三、数据删除功能设计说明

### 3.1 设计原则

删除采用**“软删除优先、物理删除受限”**策略，保证历史数据可追溯：

| 数据 | 默认操作 | 物理删除条件 | 原因 |
|------|----------|--------------|------|
| 运动员 | 停用（status=inactive） | 不允许（若有成绩） | 保留历史成绩外键 |
| 比赛 | 停用（status=inactive） | 仅无成绩的比赛 | 保留历史成绩归属 |
| 成绩 | 标记 status=deleted | 不允许 | 成绩可审计、可恢复 |

### 3.2 权限控制

- 权限层新增两个权限位：`deleteSwimmer`、`deleteMeet`，**仅 SUPER_ADMIN 可执行**；
- SCORE_ADMIN 无任何删除权限（可录成绩、可编辑成绩，不可删除）；
- 成绩删除按钮仅超级管理员可见。

### 3.3 关键行为

- **运动员停用**：`softDeleteSwimmer(id, reason)` 记录停用原因，成绩、排名不受影响；
- **比赛删除**：`deleteMeet(id)` 先检查 `hasMeetResults`，有成绩直接抛错；
- **成绩删除**：`deleteResult(id, reason)` 调用通用 `updateResult`，标记 status=deleted 并保留 previousValues。

---

## 四、等级查询功能详解

### 4.1 数据来源

- **标准文件**：`data/athlete-grade-standards/swimming-2024.json`（15,271 字节）
- **官方依据**：国家体育总局《运动员技术等级标准》（体竞字〔2024〕121号），**2025年1月1日起施行**
- **官方来源**：[国家体育总局通知页](https://www.sport.gov.cn/n4/n220/c28294142/content.html)（附件 PDF 210 页，游泳在第 108-110 页）
- **数据覆盖**：男子、女子各 18 个可授予技术等级的小项，覆盖 50 米池与 25 米池两种池型

### 4.2 数据文件结构

```json
{
  "metadata": {
    "source": "国家体育总局《运动员技术等级标准》体竞字〔2024〕121号",
    "effectiveDate": "2025-01-01",
    "version": "2024",
    "gradeOrder": ["international_master", "master", "first_class", "second_class", "third_class"],
    "gradeNames": { "international_master": "国际级运动健将", "master": "运动健将", ... }
  },
  "men": { "50m_pool": { "50_freestyle": { "international_master": 21960, ... } } },
  "women": { ... }
}
```

> 存储单位为毫秒整数；`21960` = 21.96 秒。

### 4.3 判定算法（`src/algorithms/athleteGrade.js`）

| 规则 | 说明 |
|------|------|
| 成绩方向 | 时间越短越好，`timeMs <= standardTimeMs` 为达标 |
| 判定顺序 | 从国际级运动健将 → 一级 → 二级 → 三级逐级判断 |
| 最高等级 | 取达标列表中最高的一项 |
| 池型 | 支持 50m_pool / 25m_pool，默认 50 米池 |
| 特殊项 | 100 米混合泳仅 25 米池设项；男子 100 混 50 米池无国际级标准（显示“—”） |
| 扩展性 | 通过 `version` 参数预留 `swimming-2027.json` 等新版标准 |

### 4.4 页面与交互（`pages/athlete-grade.html`）

- 输入：性别（男/女）、项目（18 项下拉）、池型（50米/25米）、成绩文本
- 成绩输入兼容 `58.32` 与 `1:02.35` 两种格式（复用 `parseTime`）
- 结果：最高达标等级高亮卡片 + 各等级标准对照表（标准成绩、我的成绩、达标与否、时间差）
- 合规声明：页面明确标注“仅按成绩达标与否判定，最终以体育主管部门认定为准”，不作等级称号授予承诺

### 4.5 等级查询测试用例

| 序号 | 性别 | 项目 | 成绩 | 期望结果 | 实际结果 |
|------|------|------|------|----------|----------|
| 1 | 男 | 50米自由泳 | 21.96 | 国际级运动健将 | PASS |
| 2 | 男 | 50米自由泳 | 27.50 | 二级运动员 | PASS |
| 3 | 男 | 50米自由泳 | 32.01 | 未达标 | PASS |
| 4 | 女 | 100米自由泳 | 58.00 | 达标（具体等级见对照） | PASS |
| 5 | 男 | 100米混合泳（50米池） | 任意 | 无国际级标准显示“—” | PASS |

---

## 五、当前 localStorage 架构及限制

### 5.1 数据流架构

```
┌─────────────────────────────────────────────────┐
│                    浏览器                        │
│  ┌─────────┐   ┌──────────────┐   ┌─────────┐  │
│  │ 页面 UI  │──▶│  services    │──▶│  JSON   │  │
│  │          │   │ dataService  │   │  fetch  │  │
│  └─────────┘   └──────┬───────┘   └─────────┘  │
│                      │ 写入                      │
│               ┌──────▼───────┐                  │
│               │  writeService│                  │
│               │  localStorage│                  │
│               └──────────────┘                  │
└─────────────────────────────────────────────────┘
```

### 5.2 当前读写流程

| 操作 | 流程 |
|------|------|
| 读取 | `dataService.fetchData()` 优先读 localStorage（`swim_data_` 前缀），无覆盖时回退 fetch JSON |
| 写入 | `writeService` 先读取现有数据（localStorage 或 JSON），合并后写回 localStorage |
| 缓存 | `clearCache()` 在写入后调用，保证下次读取拿到最新数据 |
| 清除 | `clearLocalOverrides()` 可一键恢复 JSON 原始数据 |

### 5.3 当前限制（明确列出）

1. **数据仅存于单一浏览器**：localStorage 是浏览器本地存储，不同设备、不同浏览器、不同用户之间**无法共享**；
2. **管理员写入不持久化**：数据写在访问者的浏览器里，服务端 JSON 不会更新，部署到 GitHub Pages 后其他用户读不到管理员录入的成绩；
3. **无实时同步**：多设备同时操作会互相覆盖（last-write-wins）；
4. **无备份恢复**：误清浏览器缓存/隐私数据即丢失；
5. **无服务端鉴权**：角色切换仅前端演示，无法真正阻止越权；
6. **公共仓库限制**：仓库为 Public，任何敏感信息（Token/密钥）一律禁止入库。

---

## 六、跨设备数据同步 · 方案技术可行性分析

> 用户决定：本阶段**不实现**任何同步方案，先行输出专项技术研究。以下为四类方案的客观技术分析（第八节为对比表）。

### 6.1 方案 A：GitHub Actions（repository_dispatch / 受保护写入）

**原理**：利用 GitHub Actions 作为“后端”，通过 `repository_dispatch` 事件触发工作流，由工作流代写数据文件并 commit 回仓库。

| 维度 | 评估 |
|------|------|
| Token 是否暴露 | 不暴露。Token 存于仓库 Secrets（`GITHUB_TOKEN` 或 PAT），仅 Actions 运行时注入 |
| 自动同步 | 支持。前端调用 API 触发 dispatch → 工作流写数据 → 其他设备拉取更新 |
| 多设备 | 支持（数据最终在仓库 JSON，所有设备从 GitHub Pages 读取） |
| 安全性 | 需要防伪造请求（repository_dispatch 端点本身要求有仓库写权限的 Token 才能触发）；仍需校验请求来源 |
| 实现复杂度 | 中。需新增 workflow、前端 API 调用层、冲突处理（并发写） |
| 成本 | 免费额度内（GitHub Actions 免费仓库每月 2000 分钟，足够低频写入） |
| 是否需公开仓库 | 仓库必须**公开**才能用 GitHub Pages 免费托管；若需私有仓库需 GitHub Pro/Team |

**关键限制：**
1. 并发写冲突：两个用户同时触发 dispatch 会互相覆盖，需加乐观锁/版本号；
2. 写入延迟：Action 触发 + 运行 + 重新部署 GitHub Pages，通常有数十秒级延迟，非实时；
3. GitHub Pages 缓存：写入后 Pages 有缓存时间，需缓存刷新策略（如 cache-busting 参数）；
4. 仍需解决“谁有权限触发写操作”的认证问题（公开仓库意味着任何访客可读、不可写，但写入端点的鉴权仍需设计）。

**结论：技术上可行，是当前零成本方案中最接近“真同步”的路线，但非实时、有并发与防伪造成本。**

### 6.2 方案 B：GitHub App（installation token）

**原理**：注册 GitHub App，安装到仓库，换取 installation token 授权写入。

| 维度 | 评估 |
|------|------|
| Token 是否暴露 | 前端只拿 App 的 installation token（短时效、可撤销），私钥保存在后端，不落前端 |
| 自动同步 | 支持（installation token 可写仓库，可自动 commit/push） |
| 多设备 | 支持 |
| 安全性 | 较高。installation token 仅可写该仓库；私钥在服务端，可撤销 |
| 实现复杂度 | 高。需要：注册 App → 生成私钥 → 服务端签发 token → 前端调用写数据 |
| 成本 | 免费（GitHub App 免费），但需要额外托管一个签发 token 的后端（哪怕是 Serverless 函数） |
| 是否需公开仓库 | 仓库可私有（App 安装于私有仓库；但 GitHub Pages 免费托管要求公开） |

**关键限制：**
1. 需要**一个服务端组件**来签发 installation token——纯静态 GitHub Pages 无法完成；
2. 签发流程复杂度高（JWT 签名、installation 查询、token 刷新）；
3. 若只为单机演示，实现成本远超收益。

**结论：安全性与灵活性高，但需额外的服务端资源，属于“如果要做生产级多用户写入”的首选路线。**

### 6.3 方案 C：GitHub OAuth（前端登录绑定身份）

**原理**：用 GitHub OAuth 让用户登录授权，拿到 access token 后直接调 GitHub API 写入仓库。

| 维度 | 评估 |
|------|------|
| Token 是否暴露 | **有风险**：OAuth token 出现在前端 JavaScript 中，等于公开给所有访问者（含机器人），不可用于写入 |
| 自动同步 | 支持（OAuth token 可直接调 API） |
| 多设备 | 支持（每个授权用户各自 token） |
| 安全性 | 前端持有写权限 token = 安全漏洞。若仅用于只读身份验证（确认身份）则可接受 |
| 实现复杂度 | 中（OAuth 流程 + token 管理） |
| 成本 | 免费 |

**关键限制：**
1. **严禁把带写权限的 OAuth token 放前端**——任何人查看网页源码即可提取并滥用，等于白送写权限；
2. 若只把 OAuth 当“身份标识”（不持有写 token），仍需其他通道执行写操作（回到方案 A/B）；
3. 个人 token 有权限边界不可控风险。

**结论：在本项目纯前端架构下，OAuth 用于写入不可行（token 暴露即失控）；仅可用于登录绑定身份展示。**

### 6.4 方案 D：Issue / PR 过渡方案

**原理**：管理员在前端导出 JSON → 在 GitHub 仓库提 Issue/PR 附数据 → 维护者合入 JSON 文件。

| 维度 | 评估 |
|------|------|
| Token 是否暴露 | 不暴露（无需写 token，用户在 GitHub 界面操作） |
| 自动同步 | 不支持（人工流程） |
| 多设备 | 支持（数据最终并入仓库） |
| 安全性 | 高（符合 GitHub 权限模型，PR 需 review 合入） |
| 实现复杂度 | 低（纯人工/半自动，无需代码） |
| 成本 | 零成本 |

**关键限制：**
1. 数据同步非自动、需人工维护；
2. 对日常小规模成绩录入来说，人工 PR 流程繁琐。

**结论：过渡或兜底可行，作为“零代码、零 Token”的最小可用同步方式。**

---

## 七、六方案对比表（同步能力全览）

| 方案 | Token 是否暴露 | 自动同步 | 多设备 | 安全性 | 实现复杂度 | 成本 | 推荐用途 |
|------|----------------|----------|--------|--------|------------|------|----------|
| localStorage（现状） | 无 | 无 | 单设备 | 中（仅本地） | 已实现 | 零 | 单机演示、开发阶段 |
| JSON 导入导出 | 无 | 无 | 半自动（手动搬运） | 中 | 低 | 零 | 单机备份、跨设备手动迁移 |
| Issue/PR 过渡 | 不暴露 | 不支持（人工） | 支持（并入仓库） | 高（GitHub 权限模型） | 低 | 零 | 零代码兜底、低频数据合入 |
| GitHub Actions 写入 | 不暴露（Secrets） | 支持（数十秒延迟） | 支持 | 中（需防伪造、需鉴权） | 中 | 免费额度内 | 零成本“准实时”同步首选 |
| GitHub App | 不暴露（installation token） | 支持 | 支持 | 高（可撤销、限仓库） | 高（需服务端签发） | 需后端资源 | 生产级多用户写入 |
| GitHub OAuth | 前端持有写 token=失控 | 支持 | 支持 | 低（写 token 暴露） | 中 | 免费 | 仅身份验证，不可用于写入 |

> 注：本表不评价“最好/最差”，仅按技术条件列出适用场景。

---

## 八、推荐下一步实现路线

### 阶段目标
- 短期（零成本、零后端）：保持现状 localStorage 写入 + 读取 JSON，不破坏已完成录入功能；
- 中期（有后端资源）：选用 **方案 A（GitHub Actions repository_dispatch）** 实现零成本准实时同步，仓库保持公开；
- 长期（生产级多用户）：升级 **方案 B（GitHub App）** 或引入后端服务，仓库可私有化（需处理 Pages 托管要求）。

### 具体路线

| 阶段 | 动作 | 前置条件 | 预期效果 |
|------|------|----------|----------|
| P0（现状态） | 保持 localStorage 写入；JSON + localStorage override 读取 | 无 | 单机演示稳定 |
| P1 | 数据导出/导入功能（JSON 备份） | 简单 UI | 防止清缓存丢数据，可手动跨设备迁移 |
| P2 | 实现 GitHub Actions 同步（repository_dispatch + Secrets + 防伪 token） | 决定公开仓库、设计写权限 | 多设备自动同步，延迟数十秒 |
| P3 | 引入 GitHub App 签发 installation token | 服务端（免费 Serverless） | 生产级安全写入 |
| P4 | （可选）私有仓库 + 后端数据库 | 预算/运维 | 全面生产化 |

### 安全红线（贯穿所有阶段）
1. **绝对禁止**把 GitHub Token / PAT / API Key / 密码 / GitHub App 私钥放入 GitHub Pages 前端；
2. 公共仓库不存放任何敏感信息；
3. 写入通道必须校验请求来源与操作者身份（不能仅靠前端 UI 隐藏）；
4. 禁用 `git push --force`、禁覆盖远程已有文件。

---

## 九、本阶段修改文件清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `pages/athlete-grade.html` | 新增 | 运动员技术等级查询页面 |
| `data/athlete-grade-standards/swimming-2024.json` | 新增 | 等级标准数据（18 项 × 男/女 × 2 池型） |
| `src/algorithms/athleteGrade.js` | 新增 | 等级判定算法 |
| `assets/js/app.js` | 修改 | 导航栏新增“等级查询”入口（所有角色可见） |
| `src/services/writeService.js` | 修改 | 新增 softDeleteSwimmer/hasSwimmerResults/softDeleteMeet/deleteMeet/hasMeetResults/deleteResult |
| `src/services/authService.js` | 修改 | 新增 deleteSwimmer/deleteMeet 权限（仅 SUPER_ADMIN） |
| `pages/admin-swimmers.html` | 修改 | 运动员停用操作 + 状态筛选标签 |
| `pages/admin-meets.html` | 修改 | 比赛停用/物理删除（有成绩仅停用） |
| `pages/admin-results-edit.html` | 修改 | 成绩删除（标记 deleted）/DQ |

---

## 十、测试记录

### 10.1 等级查询测试（本地 + 线上）

| 序号 | 场景 | 结果 |
|------|------|------|
| 1 | 下拉加载 18 个项目 | PASS |
| 2 | 男子 50 自由泳 21.96 → 国际级运动健将 | PASS |
| 3 | 男子 50 自由泳 27.50 → 二级运动员 | PASS |
| 4 | 男子 50 自由泳 32.01 → 未达标 | PASS |
| 5 | 男子 100 混合泳 50 米池无国际级标准（“—”） | PASS |

### 10.2 删除功能测试

| 序号 | 测试 | 结果 |
|------|------|------|
| 1 | 有成绩运动员 M001（9 项）仅显示“停用”，无“删除”按钮 | PASS |
| 2 | 有成绩运动员 M002（4 项）仅显示“停用” | PASS |
| 3 | 无成绩比赛 M003（0 项）显示“停用+删除” | PASS |
| 4 | 成绩删除后状态变“已删除”，无操作按钮 | PASS |
| 5 | “已删除”状态筛选可见 | PASS |
| 6 | GitHub Actions：Deploy to GitHub Pages（17s success） | PASS |
| 7 | GitHub Actions：Validate Data（10s success） | PASS |

### 10.3 线上验证

- 页面：https://wendy42-1.github.io/SHU-Swimming-Club/pages/athlete-grade.html
- 结果：18 项下拉、21.96 → 国际健将，均通过

---

## 十一、安全与合规声明

1. **等级查询定位**：仅按输入条件判断是否**达标**，不认定国家正式授予的技术等级称号；页面已注明以体育主管部门认定为准；
2. **数据安全**：本阶段所有写操作仅存于浏览器 localStorage，未向外部传输任何数据；仓库内无任何敏感凭据；
3. **权限演示**：三角色控制为演示级 UI 权限，生产部署前需引入服务端鉴权；
4. **GitHub 安全**：仓库为 Public，仅存放系统数据与页面代码，已确认无 Token/密钥入库（见第九节安全红线）。

---

## 十二、附：本阶段已知问题与后续事项

| 编号 | 事项 | 状态 |
|------|------|------|
| 1 | 跨设备同步：等待用户确认后实现 P2 方案 | 待定 |
| 2 | 数据导出/导入（JSON 备份）功能 | 建议后续 |
| 3 | 正式等级授予流程的合规确认（仅达标判断） | 已标注 |
| 4 | GitHub Pages 缓存策略优化（cache-busting） | 建议后续 |

---

*报告结束。系统定位：等级查询仅按输入条件判断是否达标，不能认定国家正式授予的技术等级称号。*